[package]
name = "example"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
wsn = { workspace = true, features = ["wdll"] }
panic-never = { workspace = true }
no-panic = { workspace = true, optional = true }

[features]
no-panic = ["dep:no-panic", "wsn/no-panic"]